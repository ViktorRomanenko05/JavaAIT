package homeworkLesson20;
/*
import java.util.ArrayList;
import java.util.Scanner;

public class TestPurchase {
    static Scanner scanner = new Scanner (System.in);

    public static void main(String[] args) {

        ArrayList <Purchase> purchases = new ArrayList<>();
        Purchase purchaseOne = new Purchase("Milk", 5);
        Purchase purchaseTwo = new Purchase("Cheese", 3);

        purchases.add(purchaseOne);
        purchases.add(purchaseTwo);

        Purchase.addPurchase(purchases, "Beer", 2);
        Purchase.addPurchase(purchases, "Bread", 0.8);
        Purchase.addPurchase(purchases, "Apple", 2);
        Purchase.addPurchase(purchases, "Beer", 3.85);//повтор для проверки
        Purchase.addPurchase(purchases, "Water", 1.2);

        Purchase.printer(purchases); //выводим содержимое списка на экран

        Purchase.line();

        System.out.println("Введите наименование товара для поиска");
        String item = scanner.next().trim();
        Purchase.search(purchases, item);//поиск товара по наименованию

        Purchase.line();

        System.out.println("Введите наименование товара для удаления");
        String itemDel = scanner.next().trim();
        scanner.close();
        Purchase.deleteItem(purchases, itemDel);//удаление товара по наименованию

        System.out.println("\n Обновленный список товаров: \n");

        Purchase.printer(purchases); //контрольный вывод списка после удаления элементов

        Purchase.removePurchaseByNameIterator(purchases, purchaseTwo);
        }


    }

 */
